import React from 'react';
import './Exercise.css';

const Exercise = ({ exercise }) => {
    const { name, img, time } = exercise;
    return (
        <div>


        </div>
    );
};

export default Exercise;